# Worker hostname (no scheme, no path). Example:
#   nodrix.your-account.workers.dev
NODRIX_HOST=

# Bearer token for the device whose toggle commands you want to receive.
# Get this from the nodrix UI: Project → Devices → create / view a device.
DEVICE_TOKEN=
